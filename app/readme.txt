pip install "python-jose[cryptography]"  - creating and sending the JWT
pip install passlib                      - used to hash the password that can be stored
pip install bcrypt==4.0.1                - works with passlib for hashing password
pip install alembic                      - used for data migrations and enhance the tables 
